export enum ItemConnditions {
  new = "new",
}

export enum RequestStatus {
  init,
  loading,
  success,
  error,
}

export enum SearchTypes {
  items,
  itemDetails,
}

export enum SearchResqueTypes {
  emptyData,
  error,
}
